import {mobx,observable,computed} from 'mobx';
import React, {Component} from 'react';
import ReactDOM from 'react-dom';
import {observer} from 'mobx-react';
ReactDOM.render(
	<div>this is a counter</div>,document.querySelector('#mount'));
